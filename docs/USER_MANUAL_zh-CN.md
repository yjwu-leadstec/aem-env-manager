# AEM 环境管理器 - 用户使用手册

> 版本: 0.1.0 | 最后更新: 2026-01-21

---

## 这个工具解决什么问题？

作为 AEM 开发者，您是否遇到过这些痛点？

| 😩 痛点 | ✅ 解决方案 |
|--------|-----------|
| 项目A用 Java 11，项目B用 Java 17，每次切换都要改环境变量 | **一键切换**：点一下就切换整套环境 |
| 启动 AEM 要敲一长串命令，还经常忘记参数 | **一键启动**：点击按钮就启动 |
| 不知道 AEM 是否正常运行，要手动检查 Bundle | **健康检查**：一键查看实例状态 |
| 团队成员环境配置不一致，导致"在我电脑上能跑" | **配置共享**：导出配置文件给团队 |

**一句话总结**：这个工具让你**不用再手动改环境变量、不用敲命令启动 AEM**。

---

## 5 分钟快速开始

### 第 1 步：首次打开（自动完成）

**🎯 目的**：让应用知道你电脑上装了哪些 Java、Node.js 版本

打开应用后，会自动进入初始化向导：
1. 点击 **下一步** 跟随向导
2. 应用会自动扫描你电脑上的 Java、Node.js、Maven
3. 完成向导后，**重启终端**使环境变量生效

> 💡 这是一次性操作，以后不会再出现

---

### 第 2 步：创建"环境配置"

**🎯 目的**：保存一套环境组合，以后可以一键切换回来

**什么是"环境配置"（Profile）？**
```
比如你有两个项目：
┌─────────────────────────────┐
│ 项目A：AEM 6.5              │
│ - 需要 Java 11              │
│ - 需要 Node 16              │
└─────────────────────────────┘

┌─────────────────────────────┐
│ 项目B：AEM Cloud            │
│ - 需要 Java 17              │
│ - 需要 Node 20              │
└─────────────────────────────┘

创建两个"配置"后，切换项目只需点一下。
```

**操作步骤**：
1. 点击侧边栏 **配置管理**
2. 点击 **创建配置**
3. 输入名称（如 "AEM 6.5 项目"）
4. 选择 Java 版本、Node 版本
5. 点击 **保存**

---

### 第 3 步：添加 AEM 实例

**🎯 目的**：告诉应用你的 AEM 安装在哪里，这样才能一键启动

**操作步骤**：
1. 点击侧边栏 **AEM 实例**
2. 点击 **添加实例**
3. 点击 **浏览** 按钮，选择 AEM JAR 文件所在的文件夹
4. 应用会自动识别 JAR 文件并填写名称、端口
5. 点击 **保存**

> 💡 支持的 JAR 格式：`aem-author-p4502.jar`、`aem-publish-p4503.jar`、`aem-sdk-quickstart-*.jar`

---

### 第 4 步：启动 AEM

**🎯 目的**：一键启动 AEM，开始开发

1. 在 AEM 实例页面，找到你刚添加的实例
2. 点击 **▶ 启动** 按钮
3. 等待状态变成 🟢 **运行中**
4. 点击快捷链接访问 CRXDE、包管理器等

**🎉 完成！** 现在你可以开始 AEM 开发了。

---

## 日常使用场景

### 场景 1：切换到另一个项目

**问题**：我要从项目A切换到项目B，它们用不同的 Java 版本

**解决**：
1. 在仪表盘顶部找到 **配置切换器**
2. 选择目标项目的配置
3. 完成！Java、Node 版本已自动切换

---

### 场景 2：启动/停止 AEM

**问题**：我想启动 AEM，不想敲命令

**解决**：
1. 进入 **AEM 实例** 页面
2. 点击 **▶ 启动** 或 **⏹ 停止**

**实例状态说明**：
- 🟢 运行中 - 可以访问
- 🟡 启动中 - 请等待
- ⚫ 已停止 - 点击启动
- 🔴 错误 - 查看日志排查

---

### 场景 3：检查 AEM 是否正常

**问题**：AEM 启动了，但不确定是否完全正常

**解决**：
1. 点击实例卡片上的 **健康检查**
2. 查看 Bundle 状态、内存使用情况

---

### 场景 4：分享配置给团队

**问题**：新同事加入，想让他用一样的环境配置

**解决**：

**你（导出）**：
1. 进入 **设置** > **数据管理**
2. 点击 **导出配置**
3. 把生成的文件发给同事

**同事（导入）**：
1. 进入 **设置** > **数据管理**
2. 点击 **导入配置**
3. 选择你发的文件

---

## 软件更新

应用内置了自动更新功能，确保您始终使用最新版本。

### 自动检查更新

应用会根据您的设置自动检查更新：
- **每次启动**：每次打开应用时检查
- **每天一次**：每天首次打开时检查
- **每周一次**：每周首次打开时检查

### 更新提示

当有新版本可用时：
1. 右上角会出现 🔔 更新图标
2. 点击图标查看版本详情和更新说明
3. 点击 **立即安装** 下载并安装更新
4. 安装完成后，应用会自动重启

### 手动检查更新

1. 进入 **设置** > **更新**
2. 点击 **立即检查** 按钮
3. 如果有更新，按提示操作

> 💡 更新过程中请勿关闭应用，下载完成后会自动安装

---

## 设置说明

进入 **设置** 可以调整：

| 标签页 | 设置项 | 作用 |
|--------|--------|------|
| **通用** | 主题 | 切换浅色/深色外观 |
| **通用** | 语言 | 切换界面语言 |
| **通用** | 通知 | 开启/关闭操作提醒 |
| **路径** | 扫描路径 | 如果Java/Node没被检测到，在这里添加安装目录 |
| **数据** | 导出/导入 | 备份和恢复配置数据 |
| **数据** | 重置 | 清除所有数据恢复默认 |
| **更新** | 自动检查 | 开启/关闭自动更新检查 |
| **更新** | 检查频率 | 设置自动检查的频率 |

---

## 常见问题

### Q: 切换配置后，终端里的 Java 版本没变？

**原因**：终端需要重新加载环境变量

**解决**：关闭终端，重新打开一个新的

---

### Q: AEM 启动失败？

**检查清单**：
1. ✅ 路径是否指向正确的 JAR 文件
2. ✅ 端口是否被其他程序占用（可用 `lsof -i :4502` 检查）
3. ✅ Java 版本是否与 AEM 兼容（AEM 6.5 需要 Java 8/11，AEM Cloud 需要 Java 11/17）

---

### Q: 扫描不到我安装的 Java/Node？

**解决**：
1. 进入 **设置** > **路径配置**
2. 添加你的版本管理器目录（如 `~/.sdkman/candidates/java`）
3. 点击保存后重新扫描

---

## 快捷键

| 快捷键 | 功能 |
|--------|------|
| `Cmd/Ctrl + 1` | 跳转到仪表盘 |
| `Cmd/Ctrl + 2` | 跳转到配置管理 |
| `Cmd/Ctrl + 3` | 跳转到 AEM 实例 |
| `Cmd/Ctrl + ,` | 打开设置 |

---

## 需要帮助？

- 📖 查看 `docs/` 目录下的其他文档
- 🐛 [报告问题](https://github.com/yjwu-leadstec/aem-env-manager/issues)

---

<p align="center">
  <strong>AEM 环境管理器</strong> - 让 AEM 开发更简单
</p>
